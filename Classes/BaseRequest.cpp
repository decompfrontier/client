#include "pch.h"
#include "BaseRequest.h"
#include "Utils.h"

void BaseRequest::addGumiliveParams(JsonNode* n)
{

}

void BaseRequest::replaceGroup(const char* group)
{
	for (unsigned int i = 0; i < m_groups.count(); i++)
	{
		auto obj = m_groups.getObjectAtIndex(i);
	}
}